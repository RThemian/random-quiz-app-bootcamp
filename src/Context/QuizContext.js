import { createContext } from "react";

import React from "react";

export default function QuizContextProvider() {
  return <div>Quiz Context Provider</div>;
}

export const QuizContext = createContext({});
